# 2D 软体瑜伽球物理模拟 — 技术方案

基于 `balls.html` 的实现说明。

## 总体架构

| 项 | 说明 |
| --- | --- |
| 形态 | 单文件 Web 应用（HTML + 内联 JS + Canvas 2D） |
| 依赖 | Tailwind CSS CDN（页面主体未使用其组件类） |
| 渲染 | 全屏 `<canvas id="physicsCanvas">`，`requestAnimationFrame` 主循环 |
| 构建 | 无打包、无模块，浏览器直接打开即可运行 |

页面禁用缩放与默认触摸滚动（`touch-action: none`），背景色 `#1a1a2e`。

## 物理模型：PBD + 质点多边形

采用 **Position Based Dynamics（PBD）**，通过位置约束修正代替纯力积分弹簧链，适合实时软体。

### 全局参数

| 参数 | 默认值 | 含义 |
| --- | --- | --- |
| `gravity` | `{ x: 0, y: 1500 }` | 重力加速度 |
| `dt` | `1/60` | 逻辑时间步 |
| `numSubsteps` | `10` | 每帧子步数，提高稳定性 |
| `pbdIterations` | `2` | 每个子步内约束求解轮数 |

### 数据结构

- `particles`：全部质点
- `constraints`：距离约束 + 面积约束
- `balls`：每球元数据（质点数组、半径、配色），用于渲染与碰撞

每个软球由 **40 个边缘质点** 均匀分布在圆周上，构成闭合多边形。

## 约束系统

### 1. 距离约束 `DistanceConstraint`

- **相邻顶点**（`i` ↔ `i+1`）：刚度 `1.0`，维持边长与周长。
- **隔点连接**（`i` ↔ `i+2`）：刚度 `0.8`，抑制表面锐角折叠。

求解：计算当前距离与 `restLength` 的差，按两质点 `invMass` 加权分配位置修正（标准 PBD 距离约束）。

### 2. 面积约束 `AreaConstraint`

模拟瑜伽球「充气」后的体积/气压保持：

1. 初始化时用鞋带公式记录 `restArea`。
2. 每步计算当前有符号面积，与目标面积的差 `deltaArea`。
3. 对每个顶点求面积梯度，用 PBD 乘子 `lambda` 做位置校正。

效果：软体不易被压扁，挤压后仍能回弹。

## 单帧物理管线（每个子步）

```
外力预测 → PBD（内部约束 + 球-球碰撞 + 屏幕边界）→ 速度反推 → 摩擦/阻尼 → 静止漂移抑制
```

### 1. 预测与外力

- 非拖拽质点：`vy += gravity.y * subdt`，`vx += gravity.x * subdt`，再 `x += v * subdt`。
- 保存 `px/py` 作为子步开始前的位置，供后续反推速度。

### 2. 运动学拖拽

- 交互选中的 `dragNode` 直接设为鼠标/触摸坐标，不参与重力积分，`vx/vy` 置 0。

### 3. PBD 迭代

每子步执行 `pbdIterations` 轮，每轮顺序：

1. 遍历 `constraints` 调用 `solve()`；**正序 / 逆序交替**（`constraintOrderFlip`），减轻求解偏置。
2. `solveBallCollisions()`：两球质点互相对对方多边形做穿透修正。
3. 屏幕四边硬约束：`padding = 5`，将质点夹回可视区域。

### 4. 速度更新

- `v = (x - px) / subdt`
- **底边**：向上速度弱反弹（×0.1），水平摩擦（×0.8）
- **左右墙**：水平弱反弹，竖直摩擦
- 全局空气阻力：`vx/vy *= 0.999`

### 5. 静止漂移抑制

当单球平均速度低于阈值（`driftSpeedThreshold = 40`，且 `|meanVx| < 8`）时，对各质点 `vx` 减去均值的 `2%`，减轻静止时的水平微漂。球上若有拖拽点则跳过。

## 球与球碰撞

非圆-圆刚体，而是 **质点 vs 对方多边形**：

1. 对质点找对方多边形各边的最近点（`closestPointOnSegment`）。
2. 用多边形质心确定边的外法线（`getOutwardNormal`）。
3. 点在多边形内（`pointInPolygon` 射线法）或穿透壳层 `contactSlop = 2` 时，沿法向分离；修正量按质点与边两端点的 `invMass` 分配（`projectPointVsPolygon`）。

**稳定性技巧**：

- `collisionOrderFlip`：交替「球1→球2」与「球2→球1」的求解顺序。
- `collisionPhase`：每帧轮换顶点遍历起始偏移，避免固定顺序带来的单向挤压偏置。

当前实现仅处理 `balls[0]` 与 `balls[1]` 两球。

## 交互

| 输入 | 行为 |
| --- | --- |
| 鼠标 | `mousedown` 选点，`mousemove` 更新目标，`mouseup` 释放 |
| 触摸 | `touchstart/move/end`，`preventDefault` 防滚动 |

选点规则：指针 60px 内最近的质点成为 `dragNode`。绘制时显示质点到光标的连线与白点标记。

## 渲染

每帧 `clearRect` 后对每个 `ball`：

1. 质心（顶点平均）用于渐变中心。
2. `lineTo` 闭合多边形填充（40 点已足够平滑，未用贝塞尔）。
3. **径向渐变**：`colorLight` → `colorBase`，偏移模拟 3D 高光。
4. 半透明白描边 + 椭圆高光（纯视觉）。

窗口 `resize` 时同步 `canvas.width/height` 与 `width/height` 变量。

## 实体创建 `createSoftBall`

```text
createSoftBall(cx, cy, radius, colorBase, colorLight)
```

1. 圆周生成 N=40 个 `Particle(mass=1)`。
2. 注册相邻与隔点距离约束 + 一个 `AreaConstraint`。
3. 推入 `balls` 供渲染与碰撞使用。

初始化两球：屏宽 30% / 70%、高度 30%，半径 80，粉红与青色配色。

## 类与函数索引

| 名称 | 职责 |
| --- | --- |
| `Particle` | 位置、速度、质量、`invMass`、上一位置 `px/py` |
| `DistanceConstraint` | 维持边长 |
| `AreaConstraint` | 维持面积（气压） |
| `polygonCentroid` | 多边形质心 |
| `pointInPolygon` | 点在多边形内判定 |
| `closestPointOnSegment` | 点到线段最近点 |
| `getOutwardNormal` | 边的外法线 |
| `projectPointVsPolygon` | 点-多边形穿透分离 |
| `solveBallCollisions` | 双球碰撞调度 |
| `updatePhysics` | 子步物理主逻辑 |
| `draw` / `loop` | 绘制与 RAF 循环 |

## 特点与局限

**特点**

- 无第三方物理引擎，逻辑集中、易调参。
- 子步 + 多轮 PBD + 碰撞顺序轮换，实时稳定性较好。
- 面积约束适合 2D「充气软球」观感。

**局限**

- 2D 多边形近似，非 3D 或连续介质（FEM）。
- 屏幕边界为位置夹取，非冲量式接触。
- 球间碰撞写死两球索引；约束与粒子全局扁平，扩展多球需按球分组约束与通用 N 体碰撞。
- 拖拽为单点运动学，无多点抓取或刚体手柄。

## 文件关系

```text
Bubble_ball/
├── balls.html      # 完整实现（物理 + 渲染 + 交互）
└── 技术方案.md     # 本文档
```